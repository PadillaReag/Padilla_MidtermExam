name: padilla_portfolio
description: Vaporwave-themed Flutter portfolio with Firebase + UI/UX sections
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  # Networking & APIs
  http: ^1.2.2
  url_launcher: ^6.3.0
  cached_network_image: ^3.4.1

  # UI & Fonts
  google_fonts: ^6.2.1   # ✅ Google Fonts package
  cupertino_icons: ^1.0.8

  # Firebase
  firebase_core: ^3.6.0
  cloud_firestore: ^5.4.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
